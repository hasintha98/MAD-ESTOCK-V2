<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.estock">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Notification"></activity>
        <activity android:name=".MyAccount" />
        <activity android:name=".MyProfile" />
        <activity android:name=".OrdersPage" />
        <activity android:name=".Cart"></activity>
        <activity android:name=".SignUpPage" />
        <activity android:name=".OrdersPage"></activity>
        <activity android:name=".ProductPage" />
        <activity android:name=".paymentPage" />
        <activity
            android:name=".ProductListPage"
            android:label="@string/title_activity_product_list_page"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>